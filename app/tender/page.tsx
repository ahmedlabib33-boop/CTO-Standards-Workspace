import { getLatestProjectScope, quantityLabel } from "@/lib/project-scope";
export const dynamic="force-dynamic";
export default async function TenderPage(){const rows=await getLatestProjectScope(80);return <main className="page"><section className="panel"><span className="tag">Tender Workspace</span><h1 style={{fontSize:48}}>Integrated Scope → Pricing</h1><p className="lead">BOQ quantities and SAM-CTO activity candidates arrive from the common intake register. Approved mappings can then resolve corporate material, labour, equipment and subcontract rates without re-reading the tender package separately.</p><div className="flow"><span className="node">BOQ / Drawings / Specs</span><span className="arrow">→</span><span className="node">SAM-CTO</span><span className="arrow">→</span><span className="node">Resources & Rates</span><span className="arrow">→</span><span className="node">Bid Calculation</span></div></section><section className="panel" style={{marginTop:18}}><h2>Pricing Intake Queue</h2>{!rows.length?<p className="muted">No persisted scope is available yet. Corporate rates and bid calculations remain available independently of Project Intake.</p>:<div className="table-wrap"><table><thead><tr><th>Project</th><th>CSI</th><th>Scope / Activity</th><th>Quantity</th><th>SAM-CTO</th><th>Mapping</th><th>Pricing Readiness</th></tr></thead><tbody>{rows.filter(r=>r.completeness?.has_boq).map((r,i)=><tr key={i}><td>{r.project_code}</td><td>{r.csi_code}</td><td>{r.payload?.boq_items?.[0]?.description||r.csi_title}</td><td>{quantityLabel(r.quantity_by_unit)}</td><td>{r.selected_samco_code||r.payload?.samco_candidates?.[0]?.master_code||"Unmapped"}</td><td>{Math.round((Number(r.mapping_confidence)||0)*100)}%</td><td>{r.selected_samco_code?"Ready for resource/rate resolution":"Human mapping review"}</td></tr>)}</tbody></table></div>}</section></main>}
