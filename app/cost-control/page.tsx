import { getLatestProjectScope, quantityLabel } from "@/lib/project-scope";
export const dynamic="force-dynamic";
export default async function CostControlPage(){const rows=await getLatestProjectScope(80);return <main className="page"><section className="panel"><span className="tag">Cost Control Workspace</span><h1 style={{fontSize:48}}>One Scope Identity Through Cost</h1><p className="lead">The integrated scope preserves the CSI and SAM-CTO identity used at tender and planning stages, providing the bridge to CBS/budget/actual/EAC without re-coding the same work package later.</p><div className="flow"><span className="node">SAM-CTO Scope</span><span className="arrow">→</span><span className="node">Tender Budget</span><span className="arrow">→</span><span className="node">CBS / Cost Account</span><span className="arrow">→</span><span className="node">Actual / EAC</span></div></section><section className="panel" style={{marginTop:18}}><h2>Cost Integration Queue</h2>{!rows.length?<p className="muted">No persisted intake scope yet. Cost Control remains operational from the corporate baseline.</p>:<div className="table-wrap"><table><thead><tr><th>Project</th><th>SAM-CTO</th><th>CSI</th><th>Quantity</th><th>Status</th></tr></thead><tbody>{rows.map((r,i)=><tr key={i}><td>{r.project_code}</td><td>{r.selected_samco_code||"Review mapping"}</td><td>{r.csi_code}</td><td>{quantityLabel(r.quantity_by_unit)}</td><td>{r.mapping_status.replaceAll("_"," ")}</td></tr>)}</tbody></table></div>}</section></main>}
