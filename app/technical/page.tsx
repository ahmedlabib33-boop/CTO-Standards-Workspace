import { getLatestProjectScope } from "@/lib/project-scope";
export const dynamic="force-dynamic";
export default async function TechnicalPage(){const rows=await getLatestProjectScope(60);return <main className="page"><section className="panel"><span className="tag">Technical Workspace</span><h1 style={{fontSize:48}}>Scope Evidence & Interfaces</h1><p className="lead">The Technical team receives the same controlled scope created at Project Intake, with BOQ/specification/drawing evidence and cross-document gaps visible before downstream release.</p><div className="flow"><span className="node">Intake</span><span className="arrow">→</span><span className="node">CSI Scope</span><span className="arrow">→</span><span className="node">Evidence / Conflicts</span><span className="arrow">→</span><span className="node">Technical Review</span></div></section><section className="panel" style={{marginTop:18}}><h2>Latest Integrated Scope</h2>{!rows.length?<p className="muted">No persisted intake scope yet. The workspace remains available; process a project through Project Intake after PostgreSQL migration 003 is applied.</p>:<div className="table-wrap"><table><thead><tr><th>Project</th><th>CSI</th><th>SAM-CTO</th><th>BOQ</th><th>Spec</th><th>Drawing</th><th>Conflicts</th></tr></thead><tbody>{rows.map((r,i)=><tr key={i}><td>{r.project_code}</td><td>{r.csi_code}<br/><span className="tiny">{r.csi_title}</span></td><td>{r.selected_samco_code||"Review"}</td><td>{r.completeness?.has_boq?"✓":"—"}</td><td>{r.completeness?.has_spec?"✓":"—"}</td><td>{r.completeness?.has_drawing?"✓":"—"}</td><td>{r.conflicts?.length||0}</td></tr>)}</tbody></table></div>}</section></main>}
